# Machine-local agent / tooling context — never part of the public, styles-only repo.
# [LAW:single-enforcer] the convention "committed output is styles + prompts only"
# is enforced here, once, rather than in every future agent's memory.
CLAUDE.md
AGENTS.md
.claude/
.mcp.json
mcpi.toml

# Verification artifacts (mit-verify-cv5, mit-trueness-jj1): agent-run captures,
# never committed — the contact sheet is a local before/after instrument, not repo
# output. One pattern covers every viewport variant (desktop, 360x640, ...).
contact-sheet*/

# macOS Finder noise.
.DS_Store
